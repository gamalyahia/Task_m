@model IEnumerable<Task_mangment_system.Models.TeamMember>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
            <td>
                    <a asp-action="Update" asp-route-id="@item.TeamMemberId">Update</a> |
                <a asp-action="Details" asp-route-id="@item.TeamMemberId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeamMemberId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
